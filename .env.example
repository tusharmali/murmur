# Optional: the master Apps Script Web App URL used for authentication.
# If you leave this blank, the app will prompt you to paste it on first run
# and store it in the browser (localStorage). Setting it here bakes it in for
# all visitors of your deployment.
NEXT_PUBLIC_MASTER_SCRIPT_URL=
